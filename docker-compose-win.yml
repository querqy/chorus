version: '2.4'
services:

  quickstart:
    container_name: quickstart
    build:
      context: ./quickstart
      dockerfile: Dockerfile
    platform: linux
    environment:
      - SOLR_HOST=http://solr1:8983
